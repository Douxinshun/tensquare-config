server:
  port: 9001
spring:
  application:
    name: tensquare-base #指定服务名
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/tensquare_base?useSSL=false&useUnicode=true&characterEncoding=utf8&allowMultiQueries=true
    username: root
    password: 123
    jpa:
      database: mysql
      show-sql: true
      generate-ddl: true
eureka:
  client:
    service-url:
      defaultZone: http://127.0.0.1:6868/eureka
